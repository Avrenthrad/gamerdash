{
  "name": "Lykodex",
  "install": "npm ci && npm ci --prefix discord-bot",
  "terminals": [
    {
      "name": "web",
      "command": "npm run dev -- --host",
      "description": "Vite dev server for the Lykodex web app (http://localhost:5173). External-data features hit /api proxies; set VITE_API_BASE_URL in a local .env to use the deployed proxies, or leave unset for same-origin."
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 5173
    }
  ]
}
